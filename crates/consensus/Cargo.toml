[package]
name = "consensus"
version = "0.1.0"
edition = "2021"
description = "Consensus algorithms for NRCS blockchain (PoW, PoS)"
license = "Apache-2.0"
authors = ["ByteChain <dev@bytechain.cn>"]

[dependencies]
blockchain-types = { path = "../blockchain-types" }
crypto = { path = "../crypto" }
thiserror = "1.0"
num-bigint = "0.4"
num-traits = "0.2"
rand = "0.8"
hex = "0.4"
sha2 = "0.11.0"
generic-array = "0.14"
parking_lot = "0.12"

[dev-dependencies]
criterion = "0.5"
